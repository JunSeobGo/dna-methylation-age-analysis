# SATSA 잠금 외부 검증 결과 보고서

> 상태: 평가 전 템플릿. 실제 외부평가를 한 번 실행한 뒤 산출물 값을 그대로 옮기고, 값이 불리하더라도 항목을 삭제하지 않습니다.

## 1. 평가 식별 정보

| 항목 | 기록값 | 근거 |
| --- | --- | --- |
| 평가 정책 버전 | 평가 전 | `outputs/satsa_locked_evaluation_manifest.csv` |
| 평가 시각(UTC) | 평가 전 | 동일 |
| 정책 MD5 | 평가 전 | 동일 |
| SATSA bundle MD5 | 평가 전 | 동일 |
| 표준 모델 MD5 | 평가 전 | 동일 |
| age-density 모델 MD5 | 평가 전 | 동일 |
| 최초 평가 여부 | 평가 전 | `rerun`, `rerun_reason` |

재실행이면 최초 결과를 덮어쓰지 않고 재실행 사유와 변경된 입력 MD5를 명시합니다.

## 2. 입력 데이터 품질 게이트

| 확인 항목 | 결과 | 근거 파일 |
| --- | --- | --- |
| 원본 5개 파일 공식 바이트 일치 | 평가 전 | `outputs/satsa_beta_file_audit.csv` |
| 전체 CpG 행 수 250,816 | 평가 전 | 동일 |
| 1,469개 SID header 일치 | 평가 전 | 동일 |
| 잠금 표본 447명 연결 | 평가 전 | `outputs/satsa_beta_extraction_summary.csv` |
| 모델 CpG 869개 중 95% 이상 존재 | 평가 전 | `outputs/satsa_feature_coverage.csv` |
| 표본별 결측률 5% 이하 | 평가 전 | 동일 |
| beta 값 0~1 | 평가 전 | `outputs/satsa_beta_extraction_summary.csv` |
| 최종 품질 판정 | 평가 전 | 동일 |

품질 게이트가 `FAIL`이면 아래 모델 성능 표를 만들지 않고 실패 원인만 보고합니다.

## 3. 평가 집단

| 항목 | 값 |
| --- | ---: |
| 전체 표본 | 평가 전 |
| 80세 이상 표본 | 평가 전 |
| 전체 TID 가족 cluster | 평가 전 |
| 80세 이상 TID 가족 cluster | 평가 전 |

표본 수와 cluster 수는 `outputs/satsa_locked_decision.csv`에서 옮깁니다.

## 4. 잠금 모델 성능

| 모델 | 하위집단 | n | MAE | RMSE | MedAE | R² | 평균 편향 | 잔차-연령 기울기 | 보정 절편 | 보정 기울기 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| standard | 전체 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 |
| age_density | 전체 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 |
| standard | 80세 이상 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 |
| age_density | 80세 이상 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 | 평가 전 |

모든 값은 `outputs/satsa_locked_metrics.csv`에서 옮기며 임의 반올림 전 원본 CSV를 보존합니다.

## 5. 사전 고정 모델 교체 판정

| 조건 | 사전 기준 | 관측값 | 통과 여부 |
| --- | ---: | ---: | --- |
| 전체 MAE 비열등성 | 후보−표준 ≤ 0.20년 | 평가 전 | 평가 전 |
| 80세 이상 MAE 개선 | 표준−후보 ≥ 0.50년 | 평가 전 | 평가 전 |
| 전체 절대 평균 편향 악화 | 후보−표준 ≤ 0.50년 | 평가 전 | 평가 전 |
| 80세 이상 TID bootstrap 95% CI | 하한 > 0 | 평가 전 | 평가 전 |
| 최종 모델 교체 | 네 조건 모두 통과 | 평가 전 | 평가 전 |

관측값과 통과 여부는 `outputs/satsa_locked_decision.csv`를 그대로 사용합니다. 네 조건 중 하나라도 실패하면 표준 모델을 유지합니다.

## 6. 불확실성과 진단

- TID cluster bootstrap 반복 수와 seed: 평가 전
- 80세 이상 MAE 개선 평균과 95% CI: 평가 전
- 예측-실제 나이 및 잔차 그래프에서 확인된 패턴: 평가 전
- 극단치, 비선형 잔차, calibration 이상 여부: 평가 전

근거는 `outputs/satsa_locked_tid_bootstrap.csv`와 `outputs/satsa_locked_diagnostics.png`입니다. 그래프에 보이는 패턴은 수치 판정을 대체하지 않습니다.

## 7. 결론과 적용 경계

- 사전 기준에 따른 결론: 평가 전
- 유지 또는 교체할 모델: 평가 전
- 임상 진단 사용 가능 여부: 불가
- SATSA에 맞춘 추가 튜닝 여부: 수행하지 않음
- 후속 분석: 평가 결과를 새 모델 선택에 재사용하지 않는 범위에서 결정

결과가 기대와 다르더라도 기준을 사후 변경하지 않습니다. 새로운 후보를 개발하면 SATSA가 아닌 별도의 미사용 코호트에서 다시 검증합니다.
